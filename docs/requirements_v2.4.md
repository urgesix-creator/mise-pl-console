# KOGAホールディングス海外飲食店 売上管理システム 要件定義書 v2.4

| 項目 | 内容 |
|---|---|
| プロジェクト名 | KOGAホールディングス海外飲食店 売上管理システム（仮称） |
| 作成日 | 2026年5月10日（v2.4改訂：2026年5月30日） |
| 作成者 | 比嘉俊一（KOGAホールディングス株式会社 専務取締役） |
| バージョン | v2.4（売上計算の制度変更：ネットセールス（税抜）を主入力・予算基準に統一。総売上（税込）／ランチ／ディナーは税込の独立入力。整合性チェックを桁違い警告のみに緩和。データモデル設計書 v1.7 と整合） |
| ステータス | ドラフト |

---

> **【v2.4 改訂サマリー（v2.3 → v2.4）】**
>
> データモデル設計書 **v1.7** の売上計算制度変更に整合させ、以下を改訂した。本改訂はロジック・入力項目の位置づけの変更であり、**DBスキーマ（テーブル列・制約）の変更は伴わない**。
>
> 1. **予算・売上入力ともネットセールス（税抜＝net_sales）基準**に統一。
> 2. **サービス料はネット売上に対して課す**（`service_fee = net_sales × service_fee_rate`）。
> 3. **総売上（税込）・ランチ売上・ディナー売上は税込の独立入力**とし、ネット売上と連動させない（旧「税込＝税抜＋税＋サービス料」の自動連動を廃止）。
> 4. **ランチ／ディナーは day_period 行方式を維持**（DB列変更なし）。
> 5. **客単価＝総売上（税込）÷ 客数**。
> 6. **整合性チェックは桁違い警告のみ**（税込 ÷ ネットが 2.0 超または 0.5 未満で警告、**保存は許容**）。旧「税抜＋税＝税込」の厳密チェックは廃止。
>
> 主な改訂箇所：§4.2.1（売上入力）、§4.4（目標管理の基準明記）、§4.5.6（取込整合性チェック）、§5.1（コア項目）、§5.4（国マスタ注記）。

---

## 1. プロジェクト概要

### 1.1 目的

KOGAホールディングスの海外飲食店3店舗（タイ法人2店舗・ジャカルタ法人1店舗）の日次売上・仕入・販管費を、店長・現場社員が現場でWeb経由で入力し、本部がリアルタイムに把握できるシステムを構築する。既存のExcel運用を置き換え、現場入力の標準化・本部リアルタイム把握・月次PL自動化を実現する。

### 1.2 背景

- 現状はあお季タイ・博多天神ジャカルタそれぞれが独自フォーマットのExcelで運用されている
- 店舗ごとにフォーマット・仕入先・税制が異なり、本部での横断把握に時間がかかる
- 本部がリアルタイムに業績を把握できず、異常検知が遅れる
- KOGAグループ全体のAI活用方針（Claude2層構造設定の幹部展開等）と整合した、データ駆動型経営の基盤が必要

### 1.3 目的の優先順位

1. **現場入力の標準化・負荷軽減**（最優先）
2. 本部のリアルタイム業績把握
3. 月次PLの早期化・自動化
4. 全店横断のAI分析・パターン発見

---

## 2. スコープ

### 2.1 対象範囲

- 海外飲食店3店舗（初期登録）
  - タイ法人：2店舗（店舗名は管理者UIから自由登録）
  - ジャカルタ法人：1店舗
- 将来の店舗追加に対応（店舗マスタで自由追加可能）

### 2.2 対象外

- 国内飲食店、グランピング事業（グランドーム福津等）、卸事業、整体院（筋膜カッパ）
- 資金繰り管理、人件費詳細管理、予算策定（既存Excel／別システムで継続）
- 既存「週次全店管理表」との統合（当面は並行運用、定着後に置き換え判断）
- POS連携・会計ソフト連携（将来検討の余地あり）

---

## 3. 利用者・ロール

| ロール | 主な操作 | 閲覧範囲 |
|---|---|---|
| 経営層（古賀社長・比嘉専務） | 全データ閲覧、店舗マスタ管理、目標承認 | 全店 |
| 各国法人代表（西村英朗氏等） | 自国データ閲覧、目標起案 | 自国店舗のみ |
| 店舗店長・日本人マネージャー | 日次データ入力、自店データ閲覧 | 自店のみ |
| 現場社員（現地スタッフ含む） | 日次データ入力 | 自店の入力画面のみ |
| 経理・税理士 | 月次PL閲覧、CSVエクスポート | 全店の財務データ |

---

## 4. 機能要件

### 4.1 認証・権限管理

- **認証方式**：メール＋パスワード（全ロール統一）
- 個別アカウント発行（共有ID禁止）
- パスワード要件：12文字以上、英数字記号混在
- 退職時：アカウント無効化処理（パスワード変更ではなく無効化）
- 重要操作（PL閲覧、マスタ変更等）時に2要素認証（2FA）併用
- ロールベースアクセス制御（RBAC）

【簡素化方針】アクセスログ・操作監査ログは記録しない（運用負荷軽減のため）。

### 4.2 日次入力機能

#### 4.2.1 売上入力（毎日必須）

**【v2.4：制度変更】** 売上は **ネットセールス（税抜売上）を主入力**とする。サービス料・税額は税抜売上から自動計算する。総売上（税込）は税抜と連動しない独立入力とする。

- **税抜売上（net_sales）**：**主入力値**（毎日必須）。予算比・概算粗利など業績指標の基準
- **サービス料（service_fee）**：**自動計算**（`税抜売上 × 店舗マスタのサービス料率`）
- **税額（tax_amount）**：**自動計算**（課税ベースは国マスタに従う。タイ＝税抜売上＋サービス料、インドネシア＝税抜売上＋サービス料。両国とも課税ベースは同一で、税率のみ異なる）
- **総売上（gross_sales・税込）**：**独立入力値**。税抜売上から自動算出しない（ネットと連動しない）
- 客数（customer_count）
- **客単価（avg_per_customer）**：**自動計算**（`総売上（税込）÷ 客数`。客数0の場合は表示なし）
- 昼夜区分（ランチ／ディナー）：店舗設定で切替可能。昼夜分離店ではランチ・ディナーをそれぞれ**税込で独立入力**（day_period 行方式で保持）
- 割引・インフルエンサー値引（任意項目、項目名カスタマイズ可）
- 天気・曜日・イベント（店舗設定で切替可能）
- 目標売上（税抜）との差額・達成率を画面表示

> **【入力値・自動計算値の整理（v1.7 §8.1 と整合）】**
> - **主入力**：税抜売上（net_sales）
> - **独立入力**：総売上（税込）、昼夜分離店のランチ売上（税込）／ディナー売上（税込）。いずれも税抜と一致・連動を要しない
> - **自動計算**：サービス料（＝税抜×料率）、税額（課税ベースに従う）、客単価（＝税込÷客数）
> - **整合性**：税抜と税込の厳密一致は要求せず、桁違いのみ警告（§4.5.6 参照）

#### 4.2.2 仕入入力（仕入発生日のみ任意）

- 仕入先別明細（店舗別マスタから選択）
- 仕入カテゴリ別合計（自動集計）
- 当日・累計の自動計算

#### 4.2.3 販管費入力（毎日入力）

- 給与・家賃・光熱費・減価償却・広告宣伝・車リース・会計・警備等
- 店舗別マスタで科目カスタマイズ可能

#### 4.2.4 概算棚卸し入力（任意・更新時のみ）

店舗ごとの概算棚卸金額を保持する。実地棚卸の代替として「ざっくり」の金額で運用負荷を軽減する。

- **管理単位**：店舗ごとに総額1金額（将来的にカテゴリ別へ拡張可能な構造）
- **入力場所**：日次入力画面の仕入欄の横に並列配置
- **表示形式**：「現在の概算棚卸：◯◯◯,◯◯◯ {通貨}（◯月◯日更新）」を常時表示
- **更新方法**：編集ボタンで金額を上書き更新。日々の入力は不要
- **値の保持**：店舗ごとに**最新の現在値1件のみ**（履歴は保持しない、更新時は上書き）
- **PL計算への適用**：MVPでは月次PL売上原価計算に**使用しない**（売上原価＝当月仕入合計の簡略計算）。在庫資産把握用の参考値として表示
- **権限**：店長以上が更新可能。現場社員は閲覧のみ
- **リマインダー（必須運用）**：月末日にSlack経営会議グループへ「概算棚卸の更新確認」を自動通知。月末更新がない場合、翌月1日に未更新アラートを再通知
- **強制運用設定**：上記リマインダーは初期設定で「必須」として動作。管理者権限ユーザーがシステム設定画面から「任意（オプション）」へ後日変更可能

【簡素化方針】変更履歴は記録しない。最終更新日時のみ自動記録。

#### 4.2.5 入力負荷軽減UI

- 前日値コピー機能
- デフォルト値設定
- テンプレート化（よく使う組み合わせ）
- 0円の日は自動省略
- モバイル対応（スマホ・タブレット中心、PC補完）
- 営業終了後（深夜）の入力を想定したUI

### 4.3 マスタ管理機能

| マスタ | 管理単位 | 内容 |
|---|---|---|
| 店舗マスタ | 全社 | 自由追加・編集・削除・有効/無効切替、**店舗別パラメータ（通貨・サービス料率・税制適用国・昼夜区分ON/OFF・天気入力ON/OFF・イベント入力ON/OFF）** |
| 仕入先マスタ | 店舗ごと | 完全独立、追加・廃止・並び替え可能 |
| 仕入カテゴリマスタ | 店舗ごと | 店舗ごとに自由定義（タイ：18カテゴリ、ジャカルタ：FOOD/Drink/包装/消耗品/販促品） |
| 販管費科目マスタ | 店舗ごと | 店舗ごとに自由定義 |
| 国マスタ | 全社 | 税率・税種別・税名称・税計算ロジック |
| 通貨マスタ | 全社 | JPY換算レート（手動入力、月初等に更新） |
| ユーザーマスタ | 全社 | ロール・所属店舗・有効/無効 |

### 4.4 目標管理機能

- **設定主体**：月次MTGで共同設計（本部・店長対話）→本部側で入力
- **設定頻度**：**日別（曜日変動を正確に反映するため）**
- **入力方法**：月初に当月分1ヶ月分をまとめて入力（カレンダー形式UI）
- **対象KPI**：売上のみ。**売上目標は税抜売上（ネットセールス）基準**とし、実績側の税抜売上（net_sales）と同一基準で予算比を算出する（将来的に客数・客単価・利益率等に拡張可能な構造）
- **月次集計**：日別目標の合計が月次目標として自動算出（別途月次目標を入力する必要なし）
- 日次入力画面で当日の目標と実績（いずれも税抜）の差額・達成率を表示
- **入力UX**（MVP）：カレンダーグリッドで日付ごとに目標額を入力
- **入力UX拡張**（Phase2）：「平日一括設定」「土日一括設定」「前月コピー」「曜日別パターンから自動配分」等の効率化機能

### 4.5 自動レポート・通知機能

#### 4.5.1 日報・週報（優先度1）

**日報（毎朝自動配信）**

- 配信タイミング：毎朝決まった時刻（システム設定で時刻指定可、例：日本時間9:00）
- 配信先：Slack経営会議グループ
- 報告対象：前日まで（前日の日次データが入力済みであることが前提）
- 報告内容：店舗ごとに「月初〜前日までの日別累計」を報告

**日報フォーマット仕様：**

```
【日報】YYYY年M月D日（曜日）

▼ {店舗名}（{通貨コード}）月次予算 {通貨記号}{月次予算額}
{日付}({曜日}) 売上{当日売上}／累計{累計売上}／予算比{予算比}％／粗利{当日粗利}／累計粗利{累計粗利}
{日付}({曜日}) 売上{当日売上}／累計{累計売上}／予算比{予算比}％／粗利{当日粗利}／累計粗利{累計粗利}
...（前日まで日別に出力）

▼ {次の店舗名}（同様の形式）
...
```

**指標定義：**

| 指標 | 計算式 | 備考 |
|---|---|---|
| 当日売上 | 該当日の税抜売上（net_sales） | 業績把握の標準指標 |
| 累計売上 | 月初〜該当日までの税抜売上合計 | |
| 累計予算 | SUM(daily_targets.target_sales) WHERE 月初 ≤ target_date ≤ 前日 | 日別予算の合計（曜日変動を正確に反映）。目標も税抜基準 |
| 予算比 | 累計売上 ÷ 累計予算 × 100 | 整数％表示。売上・予算とも税抜基準で同一 |
| 当日粗利（概算） | 該当日の税抜売上 − 該当日の仕入合計 | 概算値。月末棚卸更新時に月次粗利は別途調整 |
| 累計粗利（概算） | 月初〜該当日までの概算粗利合計 | 同上 |

【注意】日報の粗利は「概算値」。月次PLの正式な売上総利益（売上原価＝月初棚卸＋月間仕入−月末棚卸を使用）とは異なる旨を、日報フッターに毎回明記する。

【v2.4 注記】日報の売上・予算比は v2.3 から変更なし（従来より税抜売上＝net_sales 基準で一貫）。今回の制度変更（税抜を主入力に確定）により、入力値と日報指標の基準が完全に一致した。

**フォーマットの設計原則：**

- プレーンテキスト形式（Slack表示で崩れないため、テーブル形式は使用しない）
- 1日1行で簡潔に表示
- 通貨記号は店舗の通貨マスタから自動取得
- 数値は3桁カンマ区切り
- 店舗順は管理者設定で指定可能（デフォルトは店舗マスタの登録順）

**週報（毎週月曜朝に自動配信）**

- 週次で売上推移・対予算差・トレンド（前週比、前月同曜日比等）をSlack配信
- 詳細フォーマットはPhase2実装時に確定

#### 4.5.2 アラート通知（優先度2）

- **売上目標比 95% 未満**、客単価異常変動、入力漏れ等で即時Slack通知
- 閾値は店舗・項目ごとにカスタマイズ可能
- 入力漏れアラート：売上未入力日の翌朝に通知
- **概算棚卸更新リマインダー（必須運用）**：月末日に更新確認をSlack通知。月末更新がない場合、翌月1日に未更新アラート再通知（管理者設定で「任意」へ変更可能）

#### 4.5.3 月次PL（優先度3）

- 自動生成
- **3階層構造（連結会計の考え方）**
  - 上位（全店共通固定）：売上高／売上原価／売上総利益／販管費計／営業利益
  - 中位（全店共通固定）：給与系／賃料系／光熱費系／広告系／減価償却系／支払手数料系／その他販管費 等10科目程度
  - 下位（店舗別自由）：細目は店舗ごとに自由定義
- **売上高は税抜売上（net_sales）を基準**とする（v2.4：予算・実績と同一基準で統一）
- **売上原価計算ロジック（簡素化）**：当月の仕入合計をそのまま売上原価とする
  - 売上原価＝当月仕入合計
  - 棚卸変動を考慮しない（履歴を保持しないため）
  - 概算棚卸は資産把握用の参考値として別途表示
- CSVエクスポート（経理が会計ソフトに手動取り込み）

【注意】在庫変動が大きい月は実態とPL値が乖離する可能性があるため、月次PLは「概算PL」と位置付ける。正式な売上総利益は会計ソフト側で棚卸調整して算出する想定。

#### 4.5.4 全店比較ダッシュボード（優先度4・MVP対象外）

- ランキング・推移グラフ・トレンド分析
- データ蓄積後（数ヶ月後）にPhase2で実装

#### 4.5.5 Excel出力・ダウンロード機能

データを.xlsx形式で出力・ダウンロード可能とする。CSVではなくExcel形式（罫線・書式・複数シート対応）で出力する。

| # | 出力種別 | 対象データ | 想定利用者 | 想定用途 |
|---|---|---|---|---|
| 1 | 日次データ期間指定エクスポート | 売上・仕入・販管費（指定期間・指定店舗） | 経営層・店長 | 既存Excelに近い形式での内部分析、外部共有 |
| 2 | 月次PL | 月次PL（指定月・指定店舗） | 経理・税理士 | 会計ソフトへの取り込み、税務申告基礎資料 |
| 3 | 仕入先別明細 | 仕入先ごとの取引明細・金額集計（指定期間・指定店舗） | 経理・店長 | 仕入先との照合、買掛金管理、税務監査対応 |
| 4 | マスタ一覧 | 店舗マスタ・仕入先マスタ・カテゴリマスタ・販管費科目マスタ・ユーザーマスタ | 経営層・本部 | マスタ整備状況の確認、外部関係者への共有 |
| 5 | 全データバックアップ | 全店・全期間の全データ（マルチシート構成） | 経営層 | 災害対策、システム移行時の備え |
| 6 | **日別売上目標エクスポート** | **指定月・指定店舗の日別売上目標（税抜基準）** | **経営層・店長** | **Excelで一括編集して再取込、年間計画策定の参考、前年実績ベースの予算策定** |

**共通要件：**

- ダウンロードボタン押下→ブラウザダウンロード
- ファイル名規則：`{出力種別}_{店舗名 or 全店}_{期間}_{出力日時}.xlsx`
- 権限制御：ロールごとに出力可能な種別を制限（現場社員は出力不可、店長は自店のみ、経営層は全店）
- 既存Excel（あお季タイ・博多天神ジャカルタ）との互換性は意識するが、完全一致は目指さない（実装着手前に詳細フォーマット仕様を確定）

【簡素化方針】出力履歴は記録しない。

#### 4.5.6 Excel一括取込（インポート）機能

ダウンロードしたExcel形式のデータをテンプレートとして、一括登録・修正を可能とする。Excel出力機能とは双方向の往復互換を持つ。

**取込種別：**

| # | 取込種別 | 対象データ | 出力との対応 |
|---|---|---|---|
| 1 | 日次データ取込 | 売上・仕入・販管費（指定期間・指定店舗） | 4.5.5 #1 出力と互換 |
| 2 | **日別売上目標取込** | **指定月・指定店舗の日別売上目標** | **4.5.5 #6 出力と互換** |

**用途：**

| # | 用途 | 想定シーン |
|---|---|---|
| 1 | 既存Excelからの初期データ移行 | 既存「あお季タイ」「博多天神ジャカルタ」のExcelデータを新システムへ一括移行 |
| 2 | 過去データの後追い入力 | システム導入後、過去分のデータを遡って一括登録 |
| 3 | 一括修正・編集（何度でも実行可能） | 出力→Excelで編集→再取込するラウンドトリップ運用 |
| 4 | 年間予算の一括設定 | 12ヶ月分の日別売上目標をExcelで設計→取込 |
| 5 | 前年実績ベースの予算策定 | 前年の日次データを出力→翌年予算欄を編集→目標として取込 |

**処理方針：常に上書き（OVERWRITE）に統一**

- マッチするレコードは**常に上書き**（旧：上書き/スキップ選択方式は廃止）
- 何度でも修正取込可能：**最新の取込が常に正となる**
- 取込ファイルに含まれないレコードは**変更されない**（部分更新OK）

**マッチング条件（テーブル別）：**

| テーブル | マッチング条件 | 動作 |
|---|---|---|
| daily_sales | (store_id, business_date, day_period) | 一致レコードを上書き、無ければ新規作成 |
| daily_purchases | (store_id, business_date, supplier_id) | 同上 |
| daily_expenses | (store_id, business_date, expense_account_id) | 同上 |
| daily_targets | (store_id, target_date) | 同上 |

**処理フロー：**

1. ファイルアップロード（ドラッグ＆ドロップ または ファイル選択）
2. 取込種別の自動判定（または手動選択）
3. バリデーション実行
4. プレビュー画面表示（影響を受けるレコード数：新規作成X件、上書きY件、エラーZ件）
5. 確認ボタン押下で取込実行（UPSERT）
6. 結果レポート表示

**バリデーション項目：**

- ファイル形式（.xlsx）
- 必須カラムの存在
- データ型（日付・数値）
- 値の妥当性（負の売上等はエラー）
- 仕入先・カテゴリのマスタ照合
- マッチング条件の重複（同一ファイル内で同じキーが複数あれば最後の行が優先）
- **売上の桁違い警告（v2.4：制度変更）**：総売上（税込）÷ 税抜売上 が **2.0 超** または **0.5 未満**の場合に「桁違いの可能性」を**警告表示**する。ただし**取込・保存はブロックしない**（税抜と税込は独立入力のため、厳密一致は検証しない）。税抜売上が 0 または未入力の行は判定しない

> **【v2.4 注記】** 旧 v2.3 の「計算整合性チェック（税抜＋税＝税込、合計値の矛盾検出）」は**廃止**。総売上（税込）は税抜から自動算出しない独立入力値となったため、厳密な等式チェックは行わず、入力ミス検出を目的とした桁違い警告のみとする（データモデル設計書 v1.7 §8.1.1 と整合）。

**マスタ未登録項目の処理：**

- 取込データに未登録の仕入先名・カテゴリ名がある場合、プレビュー時にマッピング画面表示
- 選択肢：「既存マスタにマップする／新規マスタとして自動登録する／その行をスキップする」
- 一括処理オプションあり

**権限制御：**

| ロール | インポート権限 |
|---|---|
| 経営層 | 全店インポート可（日次データ・売上目標とも） |
| 各国法人代表 | 自国店舗のみインポート可 |
| 店舗店長 | 自店のみインポート可 |
| 現場社員 | インポート不可 |
| 経理・税理士 | インポート不可（閲覧・出力のみ） |

**監査・安全機能（簡素化）：**

- インポート実行前のプレビュー画面で確認（**プレビューが唯一の安全機構**）
- プレビュー画面で「すべて上書き」「すべてスキップ」等の一括操作を慎重に確認すること
- **【重要】取込実行後は元に戻せない**ため、プレビュー段階での確認が極めて重要

【簡素化方針】取込履歴・スナップショット・24時間ロールバック機能は実装しない。

### 4.6 通知配信

- 配信先：**Slack経営会議グループ**
- 形式：日報・週報・アラート（一方向アウトバウンドのみ）

---

## 5. データモデル設計方針（4レイヤー構造）

### 5.1 レイヤー1：全店共通の必須コア

- 店舗ID・日付・通貨・基準為替レート
- **売上：税抜売上（net_sales・主入力）/サービス料（自動計算）/税額（自動計算）/総売上（税込・独立入力）**
- 客数・客単価（客単価＝総売上（税込）÷ 客数）
- 仕入合計・消耗品合計・販管費合計
- **概算棚卸金額（店舗ごとに最新値1件のみを保持。履歴は持たない）**

> **【v2.4 注記】** 売上の主入力は税抜売上（net_sales）。総売上（税込）は税抜と連動しない独立入力。サービス料・税額は税抜から自動計算（詳細は §4.2.1 およびデータモデル設計書 v1.7 §8.1）。

### 5.2 レイヤー2：店舗設定で切替可能なオプション

- 昼夜区分（ON/OFF）：タイは現在OFF、ジャカルタはON、将来切替可能。ON時はランチ・ディナーを税込で独立入力（day_period 行方式で保持）
- 天気入力（ON/OFF）：タイは現在ON、ジャカルタはOFF
- イベント入力（ON/OFF）：ジャカルタON
- 割引・インフルエンサー値引（項目名カスタマイズ可）

### 5.3 レイヤー3：店舗別マスタ（完全カスタマイズ）

- **店舗マスタ：店舗ごとのパラメータレコード（通貨、サービス料率、税制適用国、昼夜区分ON/OFF、天気入力ON/OFF、イベント入力ON/OFF）**
- 仕入先マスタ：店舗ごと完全独立
- 仕入カテゴリマスタ：店舗ごと自由定義
- 販管費科目マスタ：店舗ごと自由定義
- 目標マスタ：店舗ごと月次設定

### 5.4 レイヤー4：国マスタ（税制対応）

| 国 | 税名称 | 税率 | 課税ベース |
|---|---|---|---|
| タイ | VAT | 7% | 税抜売上＋サービス料 |
| インドネシア | PB1（JKT地方税） | 10% | 税抜売上＋サービス料 |

> **【v2.4 注記】** 税額は課税ベースを基準に自動計算する。タイ・インドネシアとも `(税抜売上 + サービス料) × 税率`（課税ベースは同一、税率のみタイ7%・インドネシア10%で異なる）。サービス料は `税抜売上 × サービス料率`（ネット売上に課す）。データモデル設計書 v1.7 §8.1 と整合。課税ベースの内部値はタイ・インドネシアとも `net_plus_service`（`net_sales` は日本・台湾等の将来用）。

---

## 6. 非機能要件

### 6.0 UI共通ルール

#### 達成率の表示（システム全体共通）

達成率（目標比）の表示色は以下のルールでシステム全体で統一する：

| 達成率 | 色 | 意味 | 適用箇所 |
|---|---|---|---|
| **100% 以上** | 緑（emerald） | 達成 | バッジ・バー・統計カード等すべて |
| **95% 以上 100% 未満** | 黒（slate-900）/ 中立 | ほぼ達成（許容圏） | 同上 |
| **95% 未満** | 朱色（rose） | 要注意 | 同上、アラート通知も連動 |

この閾値はアラート通知の閾値と同期する（4.5.2 アラート通知）。Phase2でユーザー設定によるカスタマイズが可能になる。

#### 数値の表記ルール

| 項目 | ルール |
|---|---|
| 金額 | 3桁カンマ区切り、通貨記号（฿、Rp、¥）を前置 |
| パーセント | 整数表示（小数点なし） |
| 客数 | 整数表示、単位「名」 |
| 客単価 | 整数表示、3桁カンマ区切り |
| 日付 | YYYY/MM/DD または「○月○日」 |

### 6.1 セキュリティ

- 全通信HTTPS暗号化
- パスワードハッシュ化保存（bcrypt等）
- 重要操作時の2FA
- 退職時の即時アカウント無効化手順
- 税理士等社外関係者向け：IPアドレス制限・閲覧範囲限定の検討

【簡素化方針】アクセスログ・操作監査ログは実装しない。

### 6.2 性能

- ページロード：3秒以内
- 日報生成：5分以内
- 同時接続：最大20名想定
- **Excel出力生成：5秒以内（小規模データ：1ヶ月・1店舗）、30秒以内（全データバックアップ：全店全期間）**
- **Excel一括取込：1000行までを30秒以内に取込完了（バリデーション含む）**

### 6.3 可用性

- **オンラインのみ**（オフライン入力モード不要）
- 通信障害時は入力不可（運用ガイドで明示）
- クラウドサービスの稼働率に準拠（99.9%程度）

### 6.4 多通貨対応

- 入力通貨：THB（タイ）／IDR（インドネシア）
- 表示通貨：原通貨＋JPY換算
- レート：管理者が手動入力（月末等に更新）

#### 為替レート運用ルール（重要）

**「月末レート」として運用：1ヶ月1レート、当月の全データに統一適用**

| 観点 | ルール |
|---|---|
| 概念モデル | 当月内の全データは、登録されている**最新のレート1値**で換算 |
| 月途中の更新 | 月途中でレートを更新した場合、**当月の過去データも遡って新レートで再計算** |
| 履歴管理 | 過去レートの履歴は保持しない（簡素化方針） |
| 月跨ぎ | 翌月になっても自動切替なし。実務上は月末に更新する運用を推奨 |
| 適用箇所 | 月次PL生成・ダッシュボードのJPY換算表示・データ閲覧のJPY参考表示 |

**運用例：**

```
5月1日 月初  ：1 THB = 4.70 円 で登録
5月15日      ：当月の全売上が4.70円で換算表示される
5月31日 月末 ：1 THB = 5.00 円 に更新
            → 5月1日〜31日の全データが遡って5.00円換算となる
            → 月次PLは5.00円で集計
```

**運用上の判断ポイント：**

- 月内のJPY換算表示は「暫定値」として位置付け
- 月末確定のJPY換算は「月末レート更新後」が正
- 簡略化のため、月内の細かいレート変動は反映しない

### 6.5 多言語対応

**対応言語：日本語・英語・タイ語・インドネシア語の4言語**

| 言語 | コード | 主な利用シーン |
|---|---|---|
| 日本語 | `ja` | 経営層・各国代表・本社経理（デフォルト） |
| 英語 | `en` | 各国の英語話者スタッフ、本社の国際メンバー |
| タイ語 | `th` | あお季タイ・AOKIロバタの現地スタッフ |
| インドネシア語 | `id` | 博多天神ジャカルタの現地スタッフ |

#### 言語設定の運用

| 項目 | ルール |
|---|---|
| 永続化 | `profiles.language` カラムに保存（アカウント単位） |
| デフォルト | `ja`（既存ユーザー）、新規招待時に管理者が指定可能 |
| 切替UI | プロフィール画面から1クリックで切替 |
| ログイン画面 | ブラウザの`Accept-Language`ヘッダから推定（保存しない） |
| エラーメッセージ | 個別ユーザーの言語で表示 |
| Slack日報 | 日本語固定（経営層向け） |

#### 翻訳対象

UIラベル・ボタン・見出し・説明文・トースト通知・バリデーションメッセージ等のすべてのユーザー向け文字列。

**翻訳対象外：**
- 店舗名・仕入先名・科目名等のマスタデータ（多言語名称管理は Phase 2 以降に検討）
- システムログ・監査ログ
- 開発者向けエラー詳細

#### 数値・日付フォーマット

`Intl.NumberFormat` / `Intl.DateTimeFormat` で言語ロケールに準拠：

| 言語 | 数値 | 日付 |
|---|---|---|
| ja | 1,234,567 | 2026年5月10日 |
| en | 1,234,567 | May 10, 2026 |
| th | 1,234,567 | 10 พฤษภาคม 2026 |
| id | 1.234.567 | 10 Mei 2026 |

#### 通貨表記

通貨記号と金額は言語によらず統一（例：฿85,600 はどの言語でも同じ表示）。

#### Phase 2以降の拡張

- マスタデータ（店舗名・仕入先名等）の多言語名称管理
- 中国語（繁体字）対応（台湾法人）
- ベトナム語等の追加対応

### 6.6 タイムゾーン

- タイ・インドネシア：日本時間-2時間
- 日報のタイムゾーン処理：店舗ローカル時間で「当日」を判定

---

## 7. 制約条件

### 7.1 開発体制

- 比嘉俊一（KOGAホールディングス専務取締役）が単独で開発
- 開発ツール：Claude Code

### 7.2 運用体制

- 比嘉俊一が運用・保守
- 障害対応も比嘉が一次対応
- **マネージドサービス中心の技術選定**（複雑なインフラ管理を避ける）

### 7.3 タイムライン

- 【未確定】MVP公開時期は次フェーズで決定

### 7.4 既存システム連携

| 連携先 | 連携有無 |
|---|---|
| Slack経営会議グループ | あり（一方向通知） |
| POS（各店舗） | なし |
| 会計ソフト（タイ・インドネシア） | なし（CSVエクスポートで代替） |
| 既存「週次全店管理表」 | なし（並行運用） |
| 経費精算GAS | なし（別管理） |

---

## 8. 移行計画

### 8.1 既存Excelからの移行

- **既存データ**：Excel一括取込機能（4.5.6）を使用して新システムに移行可能
  - あお季タイ：2025.7〜2026.5の11ヶ月分
  - 博多天神ジャカルタ：12.2025〜05.2026の6ヶ月分
- マスタデータ：既存Excelから仕入先・カテゴリ・販管費科目を抽出して新システムのマスタに登録（マスタ登録後に日次データを取込）
- 並行運用：新システム定着まで既存Excelも継続
- 切替判断：店長が新システムに慣れた時点（目安：1-2ヶ月）

【v2.4 注記】既存Excelの売上が税込ベースで管理されている場合、移行取込時に税抜売上（net_sales）を主入力として用意する必要がある。総売上（税込）は独立入力として併存可能。移行時のExcelテンプレート設計（税抜・税込の列構成）は実装着手前に確定する（残課題 #16）。

---

## 9. 残課題（次フェーズで決定）

| # | 項目 | 次フェーズ |
|---|---|---|
| 1 | MVP範囲の決定（最初の3-4週間で作る最小機能セット） | フェーズ2 |
| 2 | 技術スタック選定（フレームワーク・DB・ホスティング） | フェーズ3 |
| 3 | データモデルの詳細設計（ER図・テーブル設計） | フェーズ4 |
| 4 | 画面設計（ワイヤーフレーム） | フェーズ5 |
| 5 | タイ法人2店舗の具体的な店舗名 | 実装着手前 |
| 6 | 各店舗の具体的な仕入先リスト（既存Excelから抽出） | 実装着手前 |
| 7 | 各店舗の販管費科目リスト | 実装着手前 |
| 8 | 経営層・店長・税理士・現場社員の具体的なメールアドレスリスト | 実装着手前 |
| 9 | Slack経営会議グループのチャンネルID | 通知実装時 |
| 10 | **タイ法人2店舗のサービス料率（10%以外がある場合）** | 実装着手前 |
| 11 | **Excel出力フォーマットの詳細仕様（既存Excelとの互換性方針）** | フェーズ4-5 |
| 12 | **Excel一括取込のバリデーション仕様詳細・エラーメッセージ設計** | フェーズ4-5 |
| 13 | **既存Excelデータの移行スケジュール（取込実行タイミング・担当者）** | システム稼働後 |
| 14 | **概算棚卸の管理粒度（店舗総額のみ／カテゴリ別への拡張要否）** | 実装着手前 |
| 15 | **概算棚卸の初期値（システム導入時の各店舗の現状概算棚卸金額）** | 実装着手前 |
| 16 | **移行Excelテンプレートの売上列構成（税抜売上の主入力化に伴う税抜・税込の列設計）** | 実装着手前 |

---

## 10. 言語化フェーズの確定事項一覧

| # | 確定項目 | 内容 |
|---|---|---|
| 1 | 目的の優先順位 | 現場入力標準化＞本部リアルタイム把握＞月次PL自動化＞AI分析 |
| 2 | 対象範囲 | 海外飲食店3店舗（タイ2＋ジャカルタ1） |
| 3 | 入力者 | 日本人マネージャー・店長＋現場社員 |
| 4 | 閲覧者 | 経営層・各国代表・店長・経理／税理士の権限分離 |
| 5 | 入力項目 | 既存Excel同レベル＋共通コア＋店舗別マスタ構造 |
| 6 | 出力レポート優先順位 | 日報・週報＞アラート＞月次PL＞ダッシュボード |
| 7 | 既存システム連携 | Slack経営会議グループのみ |
| 8 | 開発・運用体制 | 比嘉専務 × Claude Code |
| 9 | 共通フォーマット設計 | 4レイヤー構造（共通コア＋オプション＋店舗別マスタ＋国マスタ） |
| 10 | 税制 | タイVAT 7%・インドネシアPB1 10% |
| 11 | 通信方式 | オンライン専用 |
| 12 | 店舗マスタ | 自由追加（手動入力）対応 |
| 13 | 通知配信先 | Slack経営会議グループ |
| 14 | 目標管理 | **共同設計・日別入力（曜日変動反映）・売上のみ（税抜基準）。月初に1ヶ月分まとめて入力** |
| 15 | 仕入入力頻度 | 仕入発生日のみ（売上は毎日必須） |
| 16 | 通貨換算 | 手動入力 |
| 17 | 認証方式 | メール＋パスワード（全ロール統一）＋重要操作2FA |
| 18 | **Excel出力機能** | **6種類（日次データ期間指定／月次PL／仕入先別明細／マスタ一覧／全データバックアップ／日別売上目標）** |
| 19 | **サービス料管理** | **店舗マスタで料率を保持。サービス料＝税抜売上（ネット）×料率を自動計算（実Excel検算では現状2店舗とも10%、店舗別管理で柔軟性確保）** |
| 20 | **Excel一括取込（インポート）機能** | **出力Excelと往復互換。常に上書き方式（マッチするレコードを上書き、無ければ新規作成）で何度でも修正取込可能。日次データ取込＋日別売上目標取込の2種類。マスタマッピング・プレビュー対応** |
| 21 | **概算棚卸し管理機能** | **店舗ごとに総額1金額を保持（最新値1件のみ）。日次入力画面の仕入欄横に並列配置。月次PL売上原価計算には使用せず資産把握用の参考値として表示。月末リマインダーは初期設定で必須運用、管理者設定で任意化に変更可能** |
| 22 | **売上計算の基準（v2.4 確定）** | **税抜売上（ネットセールス）を主入力・予算基準。サービス料＝税抜×料率。総売上（税込）・ランチ／ディナー売上は税込の独立入力でネットと非連動。客単価＝税込÷客数。整合性は桁違い警告のみ（保存は許容）** |

---

## 11. 技術スタック

### 11.1 採用方針

比嘉俊一がClaude Codeで単独開発・運用する前提で、**マネージドサービス中心・Claude Code適合性最大化**の方針で技術選定。

### 11.2 採用技術一覧

#### フロントエンド・バックエンド統合

| 層 | 技術 | バージョン |
|---|---|---|
| フレームワーク | Next.js（App Router） | 14+ |
| 言語 | TypeScript | 5+ |
| UIライブラリ | shadcn/ui + Tailwind CSS | 最新安定版 |
| フォーム管理 | React Hook Form + Zod | 最新安定版 |

#### データ・認証・ストレージ

| 層 | 技術 | 備考 |
|---|---|---|
| データベース | Supabase（PostgreSQL） | 認証・ストレージ統合 |
| 認証 | Supabase Auth | メール+パスワード、2FA対応 |
| 権限制御 | Row Level Security（RLS） | DB層で権限強制 |
| ファイル保存 | Supabase Storage | Excel取込ファイル等 |

#### 機能別パッケージ

| 用途 | パッケージ |
|---|---|
| Excel入出力 | ExcelJS |
| Slack連携 | @slack/web-api |
| 日付・タイムゾーン | date-fns + date-fns-tz |
| 金額計算（誤差防止） | decimal.js |

#### ホスティング

| 用途 | サービス | 想定コスト |
|---|---|---|
| アプリ | Vercel | 無料〜$20/月 |
| データベース | Supabase | 無料〜$25/月 |
| ドメイン | お名前.com等 | $10/年 |

### 11.3 採用根拠

1. **Claude Code適合性が極めて高い**：Claude Codeが最も訓練されているスタック。単独開発時の開発速度最大化
2. **マネージドサービス中心**：サーバー管理・OS更新・バックアップ等インフラ管理ほぼゼロ
3. **初期コスト極小**：実質月$0で開始可能、データ拡大しても月$45程度
4. **行レベルセキュリティ（RLS）でDB層から権限強制**：アプリ層のバグでも財務データが漏洩しない
5. **拡張性・将来性**：世界で最も採用されているスタックの1つ。Phase3でAI機能追加も容易

### 11.4 検討した代替案と不採用理由

| スタック | 不採用理由 |
|---|---|
| AWS Amplify | インフラ複雑、単独運用には過剰 |
| Firebase | NoSQL（Firestore）が複雑な集計に不向き |
| Convex | リアルタイム機能不要、エコシステム小 |
| PocketBase（自己ホスト） | 自己ホスティングで保守負担増 |
| Rails / Django | フルスタックフレームワークは単独開発で過剰 |

---

## 12. 開発フェーズ計画（MVP / Phase2 / Phase3）

### 12.1 フェーズ分割の方針

優先度#1「現場入力標準化・負荷軽減」と#2「本部リアルタイム業績把握」を満たす最小機能セットをMVPとし、Phase2以降で機能を拡張する段階的開発方針。

### 12.2 MVP（最初の3-4週間）：13機能

「店長が日々入力でき、本部に日報が届く」状態を実現する。

| # | カテゴリ | 機能 |
|---|---|---|
| 1 | 認証 | メール+パスワード認証 |
| 2 | マスタ | ユーザー・権限管理（5ロール） |
| 3 | マスタ | 店舗マスタ管理 |
| 4 | マスタ | 仕入先マスタ管理（店舗ごと） |
| 5 | マスタ | 仕入カテゴリマスタ管理（店舗ごと） |
| 6 | マスタ | 販管費科目マスタ管理（店舗ごと） |
| 7 | マスタ | 国マスタ・通貨マスタ |
| 8 | 入力 | 売上日次入力 |
| 9 | 入力 | 仕入日次入力（仕入先別明細） |
| 10 | 入力 | 販管費日次入力 |
| 11 | 入力 | 概算棚卸し管理 |
| 12 | 通知 | 日報Slack自動配信 |
| 13 | UX | 入力負荷軽減UI（前日値コピー等の基本機能） |

### 12.3 Phase2（MVP稼働4-6週間後）：8機能

データ蓄積後に真価を発揮する機能群と、システム置換に必要な拡張機能。

| # | 機能 | Phase2配置の理由 |
|---|---|---|
| 14 | 月次PL自動生成（3階層構造） | データ1ヶ月以上蓄積後に有効 |
| 15 | 週報Slack配信 | 日報定着後に追加 |
| 16 | アラート通知（売上目標比 95% 未満、入力漏れ） | 目標管理が安定してから |
| 17 | 目標管理機能（月次設定UI） | 月次PLとセットで使用 |
| 18 | 概算棚卸更新リマインダー（必須運用） | 棚卸運用が定着してから自動化 |
| 19 | Excel出力5種類 | 既存Excel完全置換に必要 |
| 20 | Excel一括取込（インポート） | 既存データ移行で活用 |
| 21 | 2要素認証（重要操作時） | セキュリティ強化 |

### 12.4 Phase3（将来）

データ蓄積・運用定着後の高度機能。実施時期は必要性に応じて随時判断。

| # | 機能 | 実施判断基準 |
|---|---|---|
| 22 | 全店比較ダッシュボード | 数ヶ月のデータ蓄積後 |
| - | AI分析・パターン発見 | 他機能の安定化後 |
| - | カスタムアラート閾値設定UI | アラート運用知見が溜まってから |
| - | 監査ログUI・ロールバック機能 | 内部統制要件が明確化してから |
| - | POS連携・会計ソフト連携 | 必要性が顕在化してから |

### 12.5 開発期間の見積もり

【憶測】比嘉俊一がClaude Codeで開発する前提での目安：

| フェーズ | 完全集中の場合 | 他業務と並行の場合 |
|---|---|---|
| MVP | 3-4週間 | 6-8週間 |
| Phase2 | 3-4週間 | 6-8週間 |
| Phase3 | 必要に応じて随時 | 必要に応じて随時 |

【要確認】他業務（千大M&A対応、KOGATRADE株式譲渡、海外出張、豊洲レストラン等）との兼ね合いで現実的な投下時間が変わる。

---

## 13. 改訂履歴

| 日付 | バージョン | 変更内容 | 作成者 |
|---|---|---|---|
| 2026-05-10 | v1.0 | 初版作成（言語化フェーズ完了） | 比嘉俊一 |
| 2026-05-10 | v1.1 | Excel出力機能（5種類）を追加。サービス料率の店舗別マスタ管理を追加。店舗マスタの店舗別パラメータ範囲を明確化。 | 比嘉俊一 |
| 2026-05-10 | v1.2 | Excel一括取込（インポート）機能を追加。出力ファイルとの往復互換、プレビュー・重複処理・マスタマッピング・24時間ロールバック対応。既存Excelからの移行計画を更新。 | 比嘉俊一 |
| 2026-05-10 | v1.3 | 概算棚卸し管理機能を追加。日次入力画面（仕入欄横）に配置、永続値・履歴管理・月次PL売上原価計算への適用を規定。 | 比嘉俊一 |
| 2026-05-10 | v1.4 | 概算棚卸の月末リマインダーを必須運用化（PL精度確保のため）。管理者設定で任意化への後日変更を可能とする運用設定機能を追加。 | 比嘉俊一 |
| 2026-05-10 | v1.5 | MVP範囲を確定（13機能）。Phase2（8機能）・Phase3（4+機能）のフェーズ分割計画を策定。「12. 開発フェーズ計画」セクションを新設。 | 比嘉俊一 |
| 2026-05-10 | v1.6 | 技術スタック確定：Next.js 14（App Router）+ TypeScript + shadcn/ui + Tailwind + Supabase（DB/Auth/Storage/RLS）+ Vercel。「11. 技術スタック」セクションを新設。 | 比嘉俊一 |
| 2026-05-10 | v1.7 | Slack日報フォーマット詳細仕様化。前日までの日別累計（売上・累計売上・予算比・粗利・累計粗利）を表示する形式に変更。プレーンテキスト形式、1日1行構成。 | 比嘉俊一 |
| 2026-05-10 | v1.8 | 売上予算を月次→日別管理に仕様変更（曜日変動を正確に反映するため）。月初にカレンダーUIで1ヶ月分一括入力。月次目標は日別合計から自動算出。予算比計算を線形配分から日別合計方式に変更。 | 比嘉俊一 |
| 2026-05-10 | v1.9 | 監査・変更履歴機能を全廃して簡素化：(1)アクセスログ・取込履歴・出力履歴・通知履歴を全廃、(2)概算棚卸・為替レートを履歴→単一値（最新のみ保持）、(3)created_by/updated_by列を全テーブルから削除、(4)Excel取込のロールバック機能を廃止、(5)月次PL売上原価を「当月仕入合計」の簡略計算に変更。 | 比嘉俊一 |
| 2026-05-10 | v2.0 | Excel取込を「常に上書き」方式に統一（旧：上書き/スキップ選択方式は廃止）。何度でも修正取込可能、最新の取込が常に正となる運用。日別売上目標のExcel出力（6種類目）・取込（2種類目）に対応。マッチング条件をテーブル別に明確化（store_id × business_date × supplier_id 等）。 | 比嘉俊一 |
| 2026-05-10 | v2.1 | 達成率の色分け閾値を厳格化：旧「100%以上=緑／80%以上=黒／80%未満=朱」→新「100%以上=緑／95%以上=黒／95%未満=朱」。アラート通知の閾値も「目標比80%以下」→「95%未満」に同期。6.0節「UI共通ルール」を新設し、達成率表示・数値表記をシステム全体ルールとして文書化。 | 比嘉俊一 |
| 2026-05-10 | v2.2 | 為替レートの運用ルールを「月末レート」として明文化：(1) 1ヶ月1レート、当月の全データに統一適用、(2) 月途中の更新は当月過去データも遡って再計算、(3) 6.4節に運用例・判断ポイントを追加。データ整合性は単一値で確保。 | 比嘉俊一 |
| 2026-05-10 | v2.3 | 多言語対応方針を転換：日本語のみ→4言語対応（日・英・タイ・インドネシア）。6.5節を全面改訂。アカウント単位の言語設定（profiles.languageカラム）、Phase 2拡張余地（中国語繁体字等）を明記。次のフェーズで `next-intl` を採用予定。 | 比嘉俊一 |
| 2026-05-30 | v2.4 | **売上計算の制度変更**（データモデル設計書 v1.7 と整合）：(1) §4.2.1 売上入力を「税抜売上（主入力）／サービス料（自動）／税額（自動）／総売上（税込・独立入力）」の構成に変更、(2) §4.4 で売上目標を税抜基準と明記、(3) §4.5.6 の整合性チェックを「税抜＋税＝税込」の厳密チェックから「桁違い警告のみ（税込÷税抜が2.0超/0.5未満で警告、保存は許容）」に変更、(4) §5.1 コア項目の売上を税抜主入力・税込独立入力に再定義、(5) §5.4 国マスタにサービス料＝税抜×料率・税計算の基準を注記、(6) 客単価＝総売上（税込）÷客数を明記、(7) 移行Excelの税抜列設計を残課題#16に追加。**DBスキーマ（テーブル列・制約）の変更なし**。 | 比嘉俊一 |

---

**以上**
